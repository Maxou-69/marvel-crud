<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://unpkg.com/@picocss/pico@1.*/css/pico.min.css">

    <title>Document</title>
</head>

<body>
    <form method="post">
        <div>
            <label for="nom">Nom&nbsp;:</label>
            <input type="text" id="nom" name="nom" value="{{personnage.nom}}" />
            <div>
                <div>
                    <label for="description">description&nbsp;:</label>
                    <textarea id="description" name="description">{{ personnage.description }}</textarea>
                </div>
                <div>
                    <label for="photo">image&nbsp;:</label>
                    <input type="text" id="photo" name="photo" value="{{personnage.photo}}" />
                </div>
                <br>
                <label for="equipe_id">Équipe :</label>
                <select name="equipe_id" id="equipe_id">
                    {{#equipes}}
                    <option value="{{ id }}">{{ nom }}</option>
                    {{/equipes}}
                    <option value="+">Créer une nouvelle Équipe</option>
                </select>
                <br>
                <button id="create_equipe" type="submit">Sauvegarder les modifications</button>
    </form>

</body>

</html>